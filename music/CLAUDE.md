# music — домен музыки (door)

You are in `~/vlad/music/` — домен музыкальной работы Влада: треки, демки,
выступления, релизы, продвижение. A work domain, peer to `study` and
`content-factory`; born 2026-07-10 from `~/vlad/vlad-vault/vault-template.md`.

Имя агенту уровня не присвоено (реестр имён: `../vlad-vault/glossary.md`).

Read first (the brain is the vault):
- `music-vault/HOME.md` — map of the knowledge.
- `music-vault/status.md` — current state, read every session.
- `music-vault/principles.md` — conventions (vlad base + deltas).

> **Invariant (all levels):** NEVER `AskUserQuestion` / popup choice dialogs —
> questions always in plain text.

## Stage

**Structure only (2026-07-10).** Зоны и шаблоны готовы, наполнение — по мере
реальной работы. Состав медиа (демки/стемы/DAW-проекты/живые записи) не
выдумываем заранее — подпапки `media/` рождаются от факта.

## Inside

- `music-vault/` — the knowledge base (see HOME).
- `media/` — файлы музыки (аудио/видео/проекты DAW). Содержимое **вне git** —
  правила именования в `media/README.md`.
- Контент наружу (посты/клипы о музыке) домен не публикует сам — это витрина:
  `../danny-content/` (Денни), маршруты в её `routing-table.md`.
