# tasks/ — per-cycle run-data

Артефакты циклов работы (spec → plan → build → review → record). Одна папка =
один цикл (заказ/сборка).

- `mendeleev-server/` — сборка сервера Влада «Сервер Менделеев» по образцу ARUKU
  (структура + правила готовы; роли/права/баннеры — в работе).
