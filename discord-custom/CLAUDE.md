# discord-custom — домен бизнеса «Discord custom» (door)

You are in `~/vlad/discord-custom/` — бизнес-домен Влада: заказная кастомизация
Discord — сборка и оформление серверов, роли/права, боты, брендинг (иконки,
баннеры, эмодзи), модерация, интеграции. Клиентский бизнес (услуга под заказ).
A work domain, peer to `music`, `study`, `drafting` and `content-factory`; born
2026-07-20 from `~/vlad/vlad-vault/vault-template.md`.

Имя агенту уровня не присвоено (реестр имён: `../vlad-vault/glossary.md`).

Read first (the brain is the vault):
- `discord-custom-vault/HOME.md` — map of the knowledge.
- `discord-custom-vault/status.md` — current state, read every session.
- `discord-custom-vault/principles.md` — conventions (vlad base + deltas).

> **Invariant (all levels):** NEVER `AskUserQuestion` / popup choice dialogs —
> questions always in plain text.

## Stage

**Structure only (2026-07-20).** Зоны и шаблоны готовы, наполнение — по мере
реальной работы. Состав услуг, пакеты, площадки поиска клиентов и стек ботов
**не выдумываем заранее** — зоны `market/`, `clients/`, `systems/` и подпапки
`assets/` рождаются от факта.

## Inside

- `discord-custom-vault/` — the knowledge base (see HOME). Клиентский бизнес →
  есть зоны `clients/` и `positioning.md` (внешнее предложение).
- `assets/` — тяжёлая графика заказов (иконки, баннеры, эмодзи, GIF, макеты).
  Содержимое **вне git** — правила именования в `assets/README.md`; в vault —
  карточки-указатели.
- Контент наружу (посты/кейсы/промо услуги) домен не публикует сам — витрина:
  `../danny-content/` (Денни).

> Карта: [[discord-custom/discord-custom-vault/HOME|HOME]] · файлы: [[discord-custom/assets/README|assets/]] · верх: [[CLAUDE|карта неймспейса]]
