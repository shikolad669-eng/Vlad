# public/ — outward-facing artifacts (door)

You are in `~/vlad/public/` — the **outward projection** zone: finished,
audience-facing artifacts that cut **across** domains (decks, one-pagers,
public sites). A projection, not a source domain.

Read first: `HOME.md` (map of what's here). Strategy/why lives up in
`../vlad-vault/ideas/` and per-domain vaults; this folder holds the **built**
public things.

No agent of its own (безагентная проекция): artifacts are **built** in their
domains and land here; nothing is authored from inside this folder.

Distinct from siblings: `../danny-content/` = the personal-brand content
projection (agent Денни); `../vlad-vault/` = the meta-vault (knowledge).

> **Invariant (all levels):** NEVER `AskUserQuestion` / popup choice dialogs —
> questions always in plain text.

## Inside

_(пусто — первый внешний артефакт создаст свою подпапку + строку в HOME.md)_
