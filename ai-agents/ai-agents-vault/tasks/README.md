# tasks/ — per-cycle run-data

Один цикл = одна папка `<кейс>/`: spec → plan → build → review → record.
Сюда ложатся прогоны по внедрениям (сборка агента конкретному клиенту) и по
кампаниям обзвона/переписки. _(пусто — первый цикл: внедрение Татьяны
записано задним числом только в карточку клиента)_
