# ai-agents — домен бизнеса «ИИ-агенты для бизнеса» (door)

You are in `~/vlad/ai-agents/` — бизнес-домен Влада: разработка и внедрение
ИИ-агентов для бизнеса под ключ (чат-агенты для записи/поддержки/квалификации
лидов) + ежемесячное обслуживание. Клиентский бизнес, рынок — Караганда (РК).
A work domain, peer to `phyto`, `discord-custom`, `music`, `study`, `drafting`
and `content-factory`; заведён 2026-07-27 из `~/vlad/vlad-vault/vault-template.md`
слиянием отдельного Obsidian-волта `Влад бизнес` в неймспейс.

Имя агенту уровня не присвоено (реестр имён: `../vlad-vault/glossary.md`).

Read first (the brain is the vault):
- `ai-agents-vault/HOME.md` — map of the knowledge.
- `ai-agents-vault/status.md` — current state, read every session.
- `ai-agents-vault/principles.md` — conventions (vlad base + deltas).

> **Invariant (all levels):** NEVER `AskUserQuestion` / popup choice dialogs —
> questions always in plain text.

## Stage

**Первый клиент закрыт (2026-07-27):** Татьяна — агент настроен, сдан, оплачен.
Модель бизнеса, цены, воронка и стек описаны; база лидов Караганды собрана
(80 карточек: 30 салонов/барбершопов + 50 онлайн-школ и курсов). Не закрыто —
суммы сделки Татьяны и её подписка (см. `status.md`).

## Inside

- `ai-agents-vault/` — the knowledge base (see HOME). Клиентский бизнес → зоны
  `clients/` (сделки) и `market/leads/` (холодная база).
- Контент наружу (кейсы, промо услуги) домен не публикует сам — витрина:
  `../danny-content/` (Денни).

> Карта: [[ai-agents/ai-agents-vault/HOME|HOME]] · верх: [[CLAUDE|карта неймспейса]]
