# drafting — домен чертежей (door)

You are in `~/vlad/drafting/` — домен черчения Влада: чертежи деталей, узлов,
проектов; спецификации, версии, заметки о том, как чертить. A work domain, peer
to `music`, `study` and `content-factory`; born 2026-07-10 from
`~/vlad/vlad-vault/vault-template.md`.

Имя агенту уровня не присвоено (реестр имён: `../vlad-vault/glossary.md`).

Read first (the brain is the vault):
- `drafting-vault/HOME.md` — map of the knowledge.
- `drafting-vault/status.md` — current state, read every session.
- `drafting-vault/principles.md` — conventions (vlad base + deltas).

> **Invariant (all levels):** NEVER `AskUserQuestion` / popup choice dialogs —
> questions always in plain text.

## Stage

**Structure only (2026-07-10).** Зоны и шаблоны готовы, наполнение — по мере
реальной работы. Инструмент/формат (CAD-система, ручной чертёж, форматы файлов)
не фиксируется заранее — предметная раскладка `drawings/` и подпапки `cad/`
рождаются от факта, не выдумываются.

## Inside

- `drafting-vault/` — the knowledge base (see HOME).
- `cad/` — тяжёлые файлы чертежей (DWG/DXF/STEP/PDF/проекты CAD). Содержимое
  **вне git** — правила именования в `cad/README.md`; в vault — карточки-указатели.
- Контент наружу (посты/разборы о чертежах) домен не публикует сам — витрина:
  `../danny-content/` (Денни).

> Карта: [[drafting/drafting-vault/HOME|HOME]] · файлы: [[drafting/cad/README|cad/]] · верх: [[CLAUDE|карта неймспейса]]
